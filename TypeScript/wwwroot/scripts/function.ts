// Named function
function add(x, y) {
    return x + y;
}

// Anonymous function
let myAdd = function (x, y) { return x + y; }

let z = 100;

function addToZ(x, y) {
    return x + y + z;
}

function add1(x: number, y: number): number {
    return x + y;
}